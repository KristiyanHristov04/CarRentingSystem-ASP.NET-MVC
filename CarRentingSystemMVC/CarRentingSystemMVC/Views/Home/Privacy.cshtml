@{
    ViewData["Title"] = "Privacy Policy";
}
<h1 class="text-white">@ViewData["Title"]</h1>

<p class="text-white">Use this page to detail your site's privacy policy.</p>

@* <select id="countriesMenu">
    
</select> *@

@* <script>
// Might be added later on in the project!
    countries();
    async function countries() {
        let countriesUrl = 'https://restcountries.com/v3.1/all';
        let request = await fetch(countriesUrl);
        let data = await request.json();

        let allCountries = [];

        for (let item of data) {
            allCountries.push(item.name.common);
        }

        allCountries.sort((a, b) => a.localeCompare(b))
        console.log(allCountries);
        
        let countriesMenu = document.getElementById('countriesMenu');
        for (let country of allCountries) {
            let option = document.createElement('option');
            option.setAttribute('value', country);
            option.textContent = country;
            countriesMenu.appendChild(option);
        }
    }
</script> *@
